#############################################
# Development (Vite dev proxy)
#############################################
# The dev server will forward /api requests to https://safir.bale.ai
# and inject this header server-side so it is NOT exposed to the browser.
# Copy this file to `.env.local` and fill the key below if you want a fallback
# value. The UI configuration menu can override this per-browser.

API_ACCESS_KEY=put-your-safir-api-key-here

# Optional default bot id shown in the UI
VITE_DEFAULT_BOT_ID=1234567890

#############################################
# Direct mode (client sends header - not recommended)
#############################################
# If you cannot use the dev proxy and must call the API directly from the browser,
# set the following variables in `.env.local`. This exposes the key to the client,
# so only use in trusted environments.

# VITE_DIRECT_API=true
# VITE_API_ACCESS_KEY=put-your-safir-api-key-here
